<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Skills Match</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header class="row align-items-center justify-content-between p-2">
      <div class="col-sm-6">
        <h1>
          <span role="img" aria-label="apple">🍎</span>
          <a href="/">Skills Match</a>
        </h1>
      </div>
      <div class="col-sm-6 text-right">
        <nav>
          <!-- Conditionally render login or logout links -->
          {{#if logged_in}}
          <a href="/profile">Profile</a> |
          <a href="/teach">All Teachers</a> |
          <a href="/learn">All Learners</a> |
          <a href="/matches">Matches</a> |
          <button class="btn btn-primary" id="logout">Logout</button>
          {{else}}
          <a href="/signup">Sign Up</a> |
          <a href="/login">Login</a>
          {{/if}}
        </nav>
      </div>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer class="row justify-content-center align-items-center p-2 border-top mt-5">
      <div class="col-sm-6 text-center mt-3">
        <p>© Spicy Pencils 2023</p>
      </div>
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>